spring:
  security:
    oauth2:
      client:
        registration:
          google:
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"



  datasource:
    url: jdbc:mysql://localhost:3306/tripverse
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}